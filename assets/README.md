# Image assets

Drop real photography here and wire it into index.html:

- `before.jpg` / `after.jpg` — hero reveal slider.
  In index.html, replace the CSS gradients on `.reveal__after` and
  `.reveal__before` with: `background-image:url('assets/after.jpg');`
- `george.jpg` — portrait in the About section (4:5 ratio looks best).
- `og-cover.jpg` — 1200×630 social share image.

Optimize to WebP where you can (you already do this on STL Home Journey).
